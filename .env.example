# Docker Compose Project Name (prevents using directory name)
COMPOSE_PROJECT_NAME=meal-planner

# Development Database Configuration (Docker)
DEV_POSTGRES_DB=your_database_name
DEV_POSTGRES_USER=your_database_user
DEV_POSTGRES_PASSWORD=your_secure_password
DEV_POSTGRES_PORT=5455

# Flask Configuration
FLASK_ENV=development
FLASK_APP=app:create_app
SECRET_KEY=generate-a-secure-secret-key
JWT_SECRET_KEY=generate-a-secure-jwt-key
JWT_ACCESS_TOKEN_EXPIRES=86400  # 24 hours in seconds

# Development Database URL (for local development)
DEV_DATABASE_URL=postgresql://your_user:your_password@localhost:5455/your_database_name

# Test Database Configuration (for pytest)
# Legacy variable for backward compatibility
TEST_DATABASE_URL=postgresql://your_user:your_password@localhost:5456/your_test_database_name

# Local test database (Docker)
TEST_DATABASE_URL_LOCAL=postgresql://your_user:your_password@localhost:5456/your_test_database_name
